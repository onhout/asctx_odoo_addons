<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="asc_therapeutics_customize.list">
      <field name="name">asc_therapeutics_customize list</field>
      <field name="model">asc_therapeutics_customize.asc_therapeutics_customize</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="asc_therapeutics_customize.action_window">
      <field name="name">asc_therapeutics_customize window</field>
      <field name="res_model">asc_therapeutics_customize.asc_therapeutics_customize</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="asc_therapeutics_customize.action_server">
      <field name="name">asc_therapeutics_customize server</field>
      <field name="model_id" ref="model_asc_therapeutics_customize_asc_therapeutics_customize"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="asc_therapeutics_customize" id="asc_therapeutics_customize.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="asc_therapeutics_customize.menu_1" parent="asc_therapeutics_customize.menu_root"/>
    <menuitem name="Menu 2" id="asc_therapeutics_customize.menu_2" parent="asc_therapeutics_customize.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="asc_therapeutics_customize.menu_1_list" parent="asc_therapeutics_customize.menu_1"
              action="asc_therapeutics_customize.action_window"/>
    <menuitem name="Server to list" id="asc_therapeutics_customize" parent="asc_therapeutics_customize.menu_2"
              action="asc_therapeutics_customize.action_server"/>
    -->
  </data>
</odoo>